# Copy to .env.local for overrides. Normally use:
#   .env.development  -> npm run dev (Vite mode development)
#   .env.production   -> npm run build (Vite mode production)

VITE_API_BASE=http://localhost:5082
